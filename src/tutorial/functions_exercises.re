/* write a function `filter` that takes a function and a list of elements and returns a new lists that only contains the  
 * elements where the function returned true
 *
 * e.g:
 *  if given a function isEven (int => bool) and a list [1, 2, 3, 4, 5, 6]
 *  it will return the list [2, 4, 6]
 *  
 */